# Copy to .env and fill in. The Claude Agent SDK also accepts an existing
# `claude` login, in which case this can be left unset.
ANTHROPIC_API_KEY=

# Optional overrides
# JOBBOT_DB=./jobbot.db
# JOBBOT_MODEL=sonnet
